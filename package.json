{
  "name": "support.vysor.io",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nunjucks '**/*.html' --path views --out dist && cp -R public/* dist"
  },
  "devDependencies": {
    "nunjucks-cli": "^1.0.0"
  }
}
